# PostgreSQL Exporter Environment Configuration
# Multiple databases separated by semicolons
DATA_SOURCE_NAME="{% for db in postgres_exporter_databases %}postgresql://{{ postgres_exporter_user }}:{{ postgres_exporter_password }}@localhost:5432/{{ db }}?sslmode=disable{% if not loop.last %};{% endif %}{% endfor %}"
