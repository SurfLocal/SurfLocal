---
# PostgreSQL Exporter defaults
# These should be overridden in the playbook or secrets file

postgres_exporter_user: "postgres"
postgres_exporter_password: "{{ platform_admin_password }}"

# Databases to monitor
postgres_exporter_databases:
  - "surf_analytics"
