apiVersion: v2
name: argo-workflows
description: Workflow engine for orchestrating parallel jobs on Kubernetes
type: application
version: 0.2.0
appVersion: "v3.5.3"
keywords:
  - workflows
  - argo
  - orchestration
  - cicd
  - automation
maintainers:
  - name: Salt Platform
home: https://argoproj.github.io/argo-workflows/
sources:
  - https://github.com/argoproj/argo-workflows
# Note: CRDs should be installed separately with kubectl apply -k ./crds/
# MinIO is now a separate chart in /helm/minio
